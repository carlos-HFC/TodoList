import { TFilterTodos, TTodos } from "../@types";

export const INITIAL_STATE_EDIT_TODO = {} as TTodos;
export const INITIAL_STATE_TODOS_FILTERED = {} as TFilterTodos;
export const INITIAL_STATE_FILTER_TODO = {
  text: "",
  type: ""
};